# Database
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgres://krieger:changeme@postgres:5432/krieger

# Better Auth — generate with: openssl rand -hex 32
AUTH_SECRET=changeme

# OIDC client secrets — generate each with: openssl rand -hex 32
FINANCE_CLIENT_SECRET=changeme

# OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
