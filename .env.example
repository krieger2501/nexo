# Database
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgres://nexo:changeme@postgres:5432/nexo

# Better Auth — generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=changeme

# OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# GitHub App (bot)
GH_APP_ID=
GH_APP_PRIVATE_KEY=
GH_WEBHOOK_SECRET=
GH_REPO_OWNER=
GH_REPO_NAME=
GITHUB_TOKEN=

# Versioning — set by CI on deploy, leave as 'dev' locally
PUBLIC_APP_VERSION=dev
